# AGENTS

## Agent skills

### Issue tracker

Issues live as local markdown under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Constraints docs

Rules that must be followed. See `docs/constraints.md`. **You must adhere to these rules when making changes.**

### Reference docs

The index of external reference material. **You must get information first from this index before consulting the actual reference material.** See `docs/agents/reference.md`.
