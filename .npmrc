; Use the public npm registry for this project, overriding any user-level
; mirror. Project-level .npmrc takes precedence over ~/.npmrc.
;
; No credentials belong in this file: it is committed. Auth for the public
; registry stays in ~/.npmrc or the environment.
registry=https://registry.npmjs.org/
