# 09: 发布就绪

**What to build:** 别人能通过 pi 的包机制装上这个扩展，并且装完就能用。README 要让他在五分钟内知道：怎么配 key、约束文件放哪、他的 markdown 会被怎么切、被拦下时那三个选项分别意味着什么。还要讲清两件容易踩的事：阈值默认值是推理出来的、应当用日志功能把它换成自己项目的数值；以及日志会原样记录文件变更内容（虽然落在项目外，但他有权知道）。

**Blocked by:** 07, 08

**Status:** done

- [x] `package.json` 的 `pi.extensions` 清单正确，扩展能被 pi 的包安装机制加载
- [x] 以本地打包方式实际安装验证一遍，确认加载无误、无运行时依赖缺失
- [x] README 说明配置四项及 `TYPESAFE_API_KEY` 的优先级
- [x] README 说明约束文件的两个位置、大小写不敏感、两处合并的行为
- [x] README 说明切分规则（标题 / bullet 归属 / 无标题按顶层 bullet），并指向 `/jev-guard` 命令用于核对切分结果
- [x] README 说明三个对话框选项的语义，以及 Esc 等于打回
- [x] README 说明默认阈值是推理值、应结合日志按自己项目标定，以及为何固定模型版本号而非使用 `jev-latest`
- [x] README 说明降级策略：一切故障与无 UI 模式均放行，本扩展不是安全边界
- [x] README 明示日志会原样记录文件变更内容，存放于 pi agent 目录

## 验证记录

本地打包安装验证已完成：

1. `npm pack` 后解包到一个真实项目的 `.pi/extensions/` 下，`pi -e` 加载成功、无缺失依赖。
2. 验证发布入口（`src/index.ts` 默认导出）确实注册了 `tool_call` handler 与 `jev-guard` 命令——这是唯一不被单元测试覆盖的文件。
3. 起本地 Jev 桩服务跑通端到端阻断路径：真 `fetch`、真文件系统、真 JSON 往返，对话框渲染正常，`reason` 带着两条规则正文回传。
